apiVersion: apps/v1
kind: Deployment
metadata:
  name: end-to-end-ci-cd-pipeline-using-github-actions
spec:
  replicas: 2
  selector:
    matchLabels:
      app: end-to-end-ci-cd-pipeline-using-github-actions
  template:
    metadata:
      labels:
        app: end-to-end-ci-cd-pipeline-using-github-actions
    spec:
      containers:
      - name: app
        image: DOCKER_USERNAME/end-to-end-ci-cd-pipeline-using-github-actions:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
